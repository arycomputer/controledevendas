
/**
 * This ruleset enforces a simple, yet secure, access model for the VendasControl application.
 * Updated to allow users to create their own profile documents.
 * Version: 1.0.3 - Allow self-creation for users and fixed purchases permissions.
 */
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // --------------------------------
    // Helper Functions
    // --------------------------------

    function isSignedIn() {
      return request.auth != null;
    }

    function isAdmin() {
       return isSignedIn() && get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'admin';
    }


    // --------------------------------
    // Collection Rules
    // --------------------------------

    match /customers/{customerId} {
      allow read, write: if isSignedIn();
    }

    match /parts/{partId} {
      allow read, write: if isSignedIn();
    }

    match /sales/{saleId} {
      allow read, write: if isSignedIn();
    }

    match /purchases/{purchaseId} {
      allow read, write: if isSignedIn();
    }

    match /serviceOrders/{serviceOrderId} {
      allow read, write: if isSignedIn();
    }
    
    match /budgets/{budgetId} {
        allow read, write: if isSignedIn();
    }

    match /discards/{discardId} {
      allow read, write: if isSignedIn();
    }

    match /users/{userId} {
      allow read: if isSignedIn();
      allow write: if isSignedIn() && (request.auth.uid == userId || isAdmin());
    }

    match /settings/{settingId} {
        allow read, write: if isSignedIn();
    }
  }
}
